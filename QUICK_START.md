# Normo - Austrian Building Code AI

## Run in 3 commands:

```bash
git clone <this-repo>
cd Normo
echo "OPENAI_API_KEY=your_key" > normo_backend/.env
docker-compose up -d
```

Open: http://localhost:3000

Ask: "What are building requirements in Austria?"

Done! 🎉